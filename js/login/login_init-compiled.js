'use strict';

document.addEventListener('DOMContentLoaded', login);

function login() {
    var loginForm = document.querySelector('.login'),
        login = new Login(loginForm);
}

//# sourceMappingURL=login_init-compiled.js.map